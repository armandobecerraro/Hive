# Editores

- **[vscode-hive](vscode-hive/)** — Extensión estándar (Extension Host de VS Code): misión + `hive_core --once`. Compatible con **VS Code, Cursor, Windsurf, VSCodium/Open VSX** y otros IDEs que carguen extensiones `.vsix` / marketplace compatible (ver [README](vscode-hive/README.md#compatibilidad-multi-ide)).
